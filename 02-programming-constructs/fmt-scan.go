package main

import "fmt"

func main() {
	/*
		var f_name, l_name string
		fmt.Println("Enter the firstName & lastName :")
		fmt.Scanln(&f_name, &l_name)
		fmt.Printf("Hi %s %s!\n", l_name, f_name)
	*/

	var no int
	fmt.Scanf("%d\n", &no)
	fmt.Println(no)
}
